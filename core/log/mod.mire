module log

extern fn rt_string_concat: (left :&str, right :&str) :str lib "c"

pub fn info: (msg :&str) {
    use dasu(rt_string_concat("[INFO] ", msg))
}

pub fn warn: (msg :&str) {
    use dasu(rt_string_concat("[WARN] ", msg))
}

pub fn error: (msg :&str) {
    use dasu(rt_string_concat("[ERROR] ", msg))
}
