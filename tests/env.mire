load kioto

@[test]
pub fn test_env_home: () :bool {
    set home = env::get("HOME")
    set l = strings::len(home)
    return l > 0
}

@[test]
pub fn test_env_cwd: () :bool {
    set d = env::cwd()
    return strings::len(d) > 0
}
