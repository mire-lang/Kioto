load kioto

@[test]
pub fn test_fs_tmp: () :bool {
    set tmpdir = "/tmp"
    return fs::exists(tmpdir)
}

@[test]
pub fn test_fs_readme: () :bool {
    set path = fs::join(env::cwd() "README.md")
    return fs::exists(path)
}
